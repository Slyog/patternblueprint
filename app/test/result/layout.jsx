export const metadata = {
  title: 'Blueprint Test Result — Pattern Blueprint Theory',
  description: 'Result view for the Blueprint Test.',
};

export default function ResultLayout({ children }) {
  return children;
}

